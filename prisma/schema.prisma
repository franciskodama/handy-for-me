generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("POSTGRES_URL")
}

model User {
  id       String   @id @default(uuid()) @db.VarChar(255)
  uid      String  @unique 
  name     String? 
  avatar   String? 
  hashedPassword String?
  createdAt DateTime @default(now()) @db.Timestamp(6)
}

model DecisionHelperList {
  id        String   @id @default(uuid()) @db.VarChar(255)
  createdAt DateTime @default(now()) @db.Timestamp(6)
  uid       String   
  list      String   
  items     DecisionHelperItem[]
}

model DecisionHelperItem {
  id        String   @id @default(uuid()) @db.VarChar(255)
  createdAt DateTime @default(now()) @db.Timestamp(6)
  uid       String   
  item      String   
  selected  Boolean   @default(true)
  list      DecisionHelperList @relation(fields: [listId], references: [id])
  listId    String
}

model VisualBoardItem {
  id        String   @id @default(uuid()) @db.VarChar(255)
  createdAt DateTime @default(now()) @db.Timestamp(6)
  uid       String   
  item      String   
  url       String
  done      Boolean
}

model BucketListItem {
  id        String   @id @default(uuid()) @db.VarChar(255)
  createdAt DateTime @default(now()) @db.Timestamp(6)
  uid       String   
  item      String   
  category  String
  done      Boolean
}

model ShortcutCategory {
  id        String   @id @default(uuid()) @db.VarChar(255)
  createdAt DateTime @default(now()) @db.Timestamp(6)
  uid       String   
  category  String   
  color     shortcut_color_enum    @default(GRAY)
  shortcuts Shortcut[]
}

enum shortcut_color_enum {
  BLUE
  GREEN
  RED
  YELLOW
  PURPLE
  ORANGE
  PINK
  TEAL
  GRAY
  BROWN
  BLACK
}

model Shortcut {
  id        String   @id @default(uuid()) @db.VarChar(255)
  createdAt DateTime @default(now()) @db.Timestamp(6)
  uid       String   
  shortcut  String   
  url       String
  description String @default("No description yet")
  categoryId  String   
  category    ShortcutCategory @relation(fields: [categoryId], references: [id])
}

model WeeklyWin {
  id        String   @id @default(uuid()) @db.VarChar(255)
  createdAt DateTime @default(now()) @db.Timestamp(6)
  uid       String   
  goal      String   
  type      String
  done      Boolean
}